{
  "name": "marketplace",
  "private": true,
  "version": "0.1.0",
  "description": "x402cloud.ai marketplace — public discovery API for x402-paid agent services",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@x402cloud/protocol": "workspace:*",
    "@x402cloud/evm": "workspace:*",
    "@x402cloud/discovery": "workspace:*",
    "@x402cloud/manifests": "workspace:*",
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250214.0",
    "typescript": "^5.7.0",
    "vitest": "^3.2.4",
    "wrangler": "^4.0.0"
  }
}
